server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/capgispringboot
    username: root
    password: Parth@#rdbms
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    serialization:
      indent_output: true